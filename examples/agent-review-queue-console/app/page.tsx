export default function Page() {
  return (
    <main style={{ maxWidth: 960, margin: "2rem auto", padding: "0 1rem", fontFamily: "system-ui, sans-serif" }}>
      <h1>Agent Review Queue Console</h1>
      <p style={{ color: "#666" }}>
        Baseline scaffold. The autonomous run will replace this page with the
        review queue, summary counts, status filter, and approve/reject/resolve
        actions per requirements.md.
      </p>
    </main>
  );
}
