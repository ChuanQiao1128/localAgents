export default function Page() {
  return (
    <main style={{ maxWidth: 720, margin: "2rem auto", padding: "0 1rem", fontFamily: "system-ui, sans-serif" }}>
      <h1>AI Writing Quality Editor</h1>
      <p style={{ color: "#666" }}>
        Baseline scaffold. The autonomous run will replace this page with the
        editor + tone selector + analyzer + suggestion list per requirements.md.
      </p>
    </main>
  );
}
