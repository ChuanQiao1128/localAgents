[project]
name = "local-agent-dev-studio"
version = "0.1.0"
description = "Local multi-agent software development studio MVP."
requires-python = ">=3.12"
dependencies = []

[project.scripts]
agent-studio = "orchestrator.cli:main"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]

